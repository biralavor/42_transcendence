# Base deps come from backend-base image.
# Add only chat-service-specific RUNTIME packages here.
# Test-only deps (pytest) live in requirements-test.txt.
python-jose[cryptography]==3.5.0
httpx==0.24.1
